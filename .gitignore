*.user
/build

# macOS
.DS_Store

# Build output (regenerated by build-macos.sh)
/Qawno.app
Server Launcher/build/

# Wine runtime installed next to the app at runtime (~180 MB) — never commit.
.Qawno/

# Telemetry override (optional QAWNO_TELEMETRY_URL for dev/staging endpoints).
# No key here anymore — telemetry goes through a server-side Edge Function.
/cmake/Telemetry.env